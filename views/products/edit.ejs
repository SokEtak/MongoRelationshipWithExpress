<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <h1>Edit Product</h1>
    <form action="/products/<%= product._id %>?_method=PUT" method="POST">
        <label for="name">Product Name</label>
        <input type="text"  name="name" placeholder="Product Name" value="<%= product.name %>">
        <br><br>

        <label for="price">Product Price($)</label>
        <input type="number" name="price" min="0" step="0.1" placeholder="Product Price" value="<%= product.price %>">
        <br><br>

        <label for="category">Category:</label>
        <select name="category" id="category">
            <!-- more on vdo:417 -->
            <% for(let category of categories) { %> 
            <!-- value pass to db  ,selected if cate of product equal to cate inside loop,value for what user see-->
               <option value="<%= category %>" <%= product.category === category ? 'selected':'' %>> <%= category %> </option>
            <% } %>
        </select>
        <br><br>

        <a href="/products/<%= product._id %>">Cancel</a>
        <input type="submit" value="Submit">
                 
    </form>
</body>
</html>